.env
.env.*
!.env.example

.claude/
.mcp.json
.mcp.json.disabled
.playwright-mcp/

proxy/certs/

node_modules/
dist/
build/
.next/
.nuxt/

coverage/
test-results/
playwright-report/

*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

.DS_Store
proxy/*.bak.*

# Local scratch files (kept on disk, not committed)
/app-screenshot.png
/calendar-login.png
/project-handoff-for-claude-code.txt

# Imported data sources — archived locally, not committed (large binaries)
/docs/data/

# 会话临时快照,不入库
/docs/SESSION_STATE.md

# DGX runner reject cache — machine-local, regenerated on demand
backend/scripts/.dgx-rejects.json

# Holiday calendar state written by the two-monthly updater — runtime data,
# not source. Kept out of git so a cron run never leaves the working tree dirty
# (which aborts the next deploy). Seed lives in backend/src/data/.
/backend/data/
